apiVersion: v1
kind: Service
metadata:
  name: demo-api
  namespace: default
spec:
  selector:
    app: demo-api
  ports:
    - protocol: TCP
      port: 8082
      targetPort: 8080
  type: LoadBalancer  # Or NodePort/ClusterIP; use Ingress for production
