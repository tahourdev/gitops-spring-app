apiVersion: v1
kind: Service
metadata:
  name: spring-app-db
  namespace: default
spec:
  selector:
    app: spring-app-db
  ports:
    - protocol: TCP
      port: 5432
      targetPort: 5432
  type: ClusterIP
